# tf-playground


## Notes

- Create ssh key-pair: `ssh-keygen -t rsa -f ~/.ssh/node-access -C {INSERT_NODE_USER}`

Example: `ssh-keygen -t rsa -f ~/.ssh/node-access -C stefanp0618`

Can create without password

- create a service account
- download key
- Set env variable: `export GOOGLE_APPLICATION_CREDENTIALS="path"`